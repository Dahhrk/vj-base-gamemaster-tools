-- control_buttons.lua
-- Handles control buttons for triggering key event stages (spawning waves, enabling changes).

local ControlButtons = {}

-- Initialize the control button interface
function ControlButtons:init()
    -- Code for setting up buttons using Onyx components
end

-- Button handlers for specific event control logic
function ControlButtons:triggerEvent(eventType)
    -- Code to send trigger signal to the backend for the specific event type
end

return ControlButtons